import Vue from 'vue';
import Revue from 'revue';
import {createStore} from 'redux';
import Transport from './containers/Transport.vue';

new Vue({
  el: '#app',
  components: {
    Transport
  }
});

