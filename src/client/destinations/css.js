// Add event at correct time
// Clear at new loop
// Concept of persistent css (per layer or grid/scene(bg)), maybe initial state?
class Css {
  addStyle(event) {
    console.log('event', event.data, event.key);
  }
}

export default Css;